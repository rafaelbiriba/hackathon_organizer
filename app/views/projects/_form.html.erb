<%= form_for(@project, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @project.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@project.errors.count, "error") %> prohibited this project from being saved:</h4>

      <ul>
      <% @project.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="panel panel-primary">
    <div class="panel-heading">
      <h3 class="panel-title"><%= f.text_field :title, class: "form-control", placeholder: "Project Title" %></h3>
    </div>
    <div class="panel-body">
      <%= f.text_area :description, rows: 10, class: "form-control", placeholder: "Project description" %>
      <hr />
      <%= f.submit class: "btn btn-primary" %>
    </div>
  </div>
<% end %>
